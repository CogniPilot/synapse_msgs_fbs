include "cerebri_topics.fbs";

namespace cerebri.log;

table SchemaRecord {
  name:string;
  bfbs:[ubyte];
}

table FlightSnapshotRecord {
  monotonic_us:ulong;
  payload:cerebri.topic.FlightSnapshot;
}

table MotorOutputRecord {
  monotonic_us:ulong;
  payload:cerebri.topic.MotorOutput;
}

union RecordPayload {
  SchemaRecord,
  FlightSnapshotRecord,
  MotorOutputRecord
}

table LogRecord {
  record:RecordPayload;
}

root_type LogRecord;
file_identifier "C2LG";
